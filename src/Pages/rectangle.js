// import React from 'react';

// const Rectangle = ({ data }) => {
//   // Extract relevant information from the data
//   const color = data.color || 'blue'; // Replace 'color' with the actual property name in your data
//   const coordinates = data.coordinates || [0, 0]; // Replace 'coordinates' with the actual property name

//   // Style for the rectangle
//   const rectangleStyle = {
//     width: '50px', // Set the width (modify as needed)
//     height: '50px', // Set the height (modify as needed)
//     backgroundColor: color, // Set the background color
//     position: 'absolute',
//     left: `${coordinates[0]}px`,
//     top: `${coordinates[1]}px`,
//   };

//   return <div style={rectangleStyle}></div>;
// };

// export default Rectangle;
